# P+F Fair Tracker Demo

Android tablet demo: front camera + on-device ML Kit face detection. The mascot's eyes and complete body smoothly follow the largest visible face. The camera image is processed live and is not stored.

Build: `gradle :app:assembleDebug`
